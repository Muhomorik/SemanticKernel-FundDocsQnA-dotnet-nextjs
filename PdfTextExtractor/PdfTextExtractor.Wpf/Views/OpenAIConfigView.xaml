<UserControl x:Class="PdfTextExtractor.Wpf.Views.OpenAIConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:viewModels="clr-namespace:PdfTextExtractor.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:OpenAIConfigViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="550" d:DesignWidth="400">
    <StackPanel>
        <!--  Optimal Settings Info Panel  -->
        <Expander
            Margin="0,0,0,10"
            IsExpanded="True"
            Header="✅ Recommended Configuration">
            <Border
                Margin="5"
                Padding="10"
                Background="{DynamicResource MahApps.Brushes.Gray10}"
                CornerRadius="5">
                <StackPanel>
                    <TextBlock
                        FontSize="12"
                        FontWeight="Bold"
                        Text="Optimal Settings:" />
                    <TextBlock
                        Margin="5,5,0,2"
                        FontSize="11"
                        Text="• Model: gpt-4o (default)" />
                    <TextBlock
                        Margin="5,0,0,2"
                        FontSize="11"
                        Text="• DPI: 150 (balance quality/cost)" />
                    <TextBlock
                        Margin="5,0,0,2"
                        FontSize="11"
                        Text="• Max Tokens: 2000 (full pages)" />

                    <Separator Margin="0,8" />

                    <TextBlock
                        Margin="0,5,0,5"
                        FontSize="12"
                        FontWeight="Bold"
                        Text="Performance:" />
                    <TextBlock
                        Margin="5,0,0,2"
                        FontSize="11"
                        Text="• Speed: Near-instant per page" />
                    <TextBlock
                        Margin="5,0,0,2"
                        FontSize="11"
                        Text="• Quality: Excellent for complex layouts" />
                    <TextBlock
                        Margin="5,0,0,5"
                        FontSize="11"
                        Text="• Requires: API key with billing enabled" />
                    <TextBlock
                        Margin="5,0,0,0"
                        FontSize="10"
                        Foreground="Gray"
                        FontStyle="Italic"
                        Text="💰 See cost estimation below" />
                </StackPanel>
            </Border>
        </Expander>

        <TextBlock
            Margin="0,10,0,10"
            FontSize="14"
            FontWeight="Bold"
            Text="OpenAI Configuration" />

        <TextBlock
            Margin="0,5,0,3"
            FontWeight="SemiBold"
            Text="OpenAI API Key:" />
        <PasswordBox
            x:Name="OpenAIApiKeyBox"
            Margin="0,0,0,10"
            PasswordChanged="OnOpenAIApiKeyChanged"
            ToolTip="Enter your OpenAI API key (sk-...)" />

        <TextBlock
            Margin="0,5,0,3"
            FontWeight="SemiBold"
            Text="Vision Model Name:" />
        <TextBox
            Margin="0,0,0,10"
            Text="{Binding OpenAIModelName, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Default: gpt-4o" />

        <TextBlock
            Margin="0,5,0,3"
            FontWeight="SemiBold"
            Text="Rasterization DPI:" />
        <DockPanel Margin="0,0,0,5">
            <StackPanel
                DockPanel.Dock="Right"
                Orientation="Horizontal">
                <Button
                    Width="40"
                    Margin="2,0"
                    Command="{Binding SetOpenAIDpiCommand}"
                    CommandParameter="150"
                    Content="150"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    ToolTip="Recommended - balance quality/cost" />
                <Button
                    Width="40"
                    Margin="2,0"
                    Command="{Binding SetOpenAIDpiCommand}"
                    CommandParameter="200"
                    Content="200"
                    ToolTip="Higher quality" />
                <Button
                    Width="40"
                    Margin="2,0"
                    Command="{Binding SetOpenAIDpiCommand}"
                    CommandParameter="300"
                    Content="300"
                    ToolTip="Best quality - higher cost" />
            </StackPanel>
            <mah:NumericUpDown
                Interval="50"
                Maximum="600"
                Minimum="72"
                Value="{Binding OpenAIDpi}" />
        </DockPanel>

        <TextBlock
            Margin="0,5,0,3"
            FontWeight="SemiBold"
            Text="Chunk Size (characters):" />
        <mah:NumericUpDown
            Margin="0,0,0,15"
            Interval="100"
            Maximum="5000"
            Minimum="100"
            Value="{Binding ChunkSize}" />

        <TextBlock
            Margin="0,5,0,3"
            FontWeight="SemiBold"
            Text="Max Tokens (vision model output):" />
        <DockPanel Margin="0,0,0,15">
            <StackPanel
                DockPanel.Dock="Right"
                Orientation="Horizontal">
                <Button
                    Width="50"
                    Margin="2,0"
                    Command="{Binding SetOpenAIMaxTokensCommand}"
                    CommandParameter="1000"
                    Content="1000"
                    ToolTip="Standard pages" />
                <Button
                    Width="50"
                    Margin="2,0"
                    Command="{Binding SetOpenAIMaxTokensCommand}"
                    CommandParameter="2000"
                    Content="2000"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    ToolTip="Full pages - recommended" />
                <Button
                    Width="50"
                    Margin="2,0"
                    Command="{Binding SetOpenAIMaxTokensCommand}"
                    CommandParameter="4000"
                    Content="4000"
                    ToolTip="Very long pages" />
            </StackPanel>
            <mah:NumericUpDown
                Interval="100"
                Maximum="16000"
                Minimum="100"
                Value="{Binding MaxTokens}" />
        </DockPanel>

        <Border
            Margin="0,10"
            Padding="10"
            Background="{DynamicResource MahApps.Brushes.Gray10}"
            CornerRadius="5">
            <StackPanel>
                <TextBlock
                    FontSize="11"
                    FontWeight="Bold"
                    Text="💰 Cost Estimation (gpt-4o):" />
                <TextBlock
                    Margin="5,5,0,2"
                    FontSize="10"
                    Text="• High detail: ~$0.003 per page" />
                <TextBlock
                    Margin="5,0,0,2"
                    FontSize="10"
                    Text="• Low detail: ~$0.0002 per page" />
                <TextBlock
                    Margin="5,0,0,2"
                    FontSize="10"
                    Text="• 10 pages ≈ $0.03 (high) / $0.002 (low)" />
                <TextBlock
                    Margin="5,0,0,0"
                    FontSize="10"
                    Foreground="Orange"
                    Text="⚠️ Requires API key with billing enabled" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
